#pragma once

class Mesh {
public:
	//Currently doesn't contain any data
	//Would contain information about which mesh to render, for now box is enough so no point of making it object specific
	Mesh ();
	void Render ();
};